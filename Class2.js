
// assignment: 02: Write a program that calculates the area of a rectangle using length and width variables.

function area(lengths, width) {
    console.log("Area of a rectangle =", lengths * width);
}
let lengths = 20;
let width = 40;
area(lengths, width);
